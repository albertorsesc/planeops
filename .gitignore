# local-only thinking and audit notes (hygiene-ok: gitignore is the sanctioned place to reference this path)
.scratch/

# generated machine state (commit is opt-in per SPEC; default ignored)
observed/

# real MCP source list is instance-specific; only the .example ships
mcp-sources.yaml

# real stackfile import mapping is instance-specific; only the .example ships
stackfile-mapping.yaml

# python
.venv/
__pycache__/
*.egg-info/
